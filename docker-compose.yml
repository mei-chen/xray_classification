# =============================================================================
# Docker Compose for Chest X-Ray Classification
# =============================================================================
# 
# Usage:
#   docker-compose up --build        # Build and run
#   docker-compose up -d             # Run in detached mode
#   docker-compose logs -f           # View logs
#   docker-compose down              # Stop and remove containers

version: '3.8'

services:
  # ---------------------------------------------------------------------------
  # Main API Service
  # ---------------------------------------------------------------------------
  xray-api:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    image: xray-classifier:latest
    container_name: xray-classifier-api
    restart: unless-stopped
    
    ports:
      - "8000:8000"
    
    volumes:
      # Mount models directory for easy model updates
      - ./models:/app/models:ro
      # Mount configs for configuration changes
      - ./configs:/app/configs:ro
    
    environment:
      - PYTHONUNBUFFERED=1
      - LOG_LEVEL=INFO
    
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    
    # Resource limits (adjust based on your hardware)
    deploy:
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 2G

  # ---------------------------------------------------------------------------
  # Optional: Nginx Reverse Proxy (for production)
  # ---------------------------------------------------------------------------
  # nginx:
  #   image: nginx:alpine
  #   container_name: xray-nginx
  #   restart: unless-stopped
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #     - ./certs:/etc/nginx/certs:ro
  #   depends_on:
  #     - xray-api

networks:
  default:
    name: xray-network

